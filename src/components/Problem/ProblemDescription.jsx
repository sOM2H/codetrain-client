import React from 'react';

const ProblemDescription = ({ description }) => (
  <div className='problem-description'>
    <p className='problem-description-text'>{description}</p>
  </div>
);

export default ProblemDescription;
